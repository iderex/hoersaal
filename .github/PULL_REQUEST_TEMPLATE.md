## What changed

<!-- One topic. If there are two, there are two pull requests. -->

## What failure it prevents

<!-- Where this is a correction, say what was wrong and how it was found. -->

Closes #

## What was run

<!-- The commands, with their output, at the commit being pushed. A claim that
the checks pass without the command that produced that verdict is the thing this
line exists to stop. -->

    command
    output

## The means

<!-- Only where this change introduces a language, a runtime, a format or a
dependency the tree does not already carry: name it and say why it fits. Delete
this section otherwise. -->
